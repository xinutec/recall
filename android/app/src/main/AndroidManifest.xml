<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <!-- API 34+: the typed foreground-service permission for mic capture. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
    <!-- API 33+: required to show the ongoing-capture notification. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Restart the stream automatically after a reboot. -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <!-- Fire the "recording resumes soon" warning at an exact wall-clock time, even in
         doze, so it keeps its full lead. USE_EXACT_ALARM (API 33+) is auto-granted;
         SCHEDULE_EXACT_ALARM covers 26-32 (granted on install there). -->
    <uses-permission
        android:name="android.permission.SCHEDULE_EXACT_ALARM"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@style/Theme.RecallMic">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Share target: another app (the mp3 recorder) can Share an audio file to
             "Recall", which uploads it to the host as a new session. -->
        <activity
            android:name=".ShareActivity"
            android:exported="true"
            android:label="Recall"
            android:excludeFromRecents="true"
            android:theme="@style/Theme.RecallMic">
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="audio/*" />
            </intent-filter>
        </activity>

        <!-- Hosts and device identity, off the daily screen. Reached from the drawer. -->
        <activity
            android:name=".SettingsActivity"
            android:exported="false"
            android:label="Settings" />

        <!-- The meeting recorder's screen. Reached from the main screen and from its own
             ongoing notification; not a second launcher icon. -->
        <activity
            android:name=".MeetingActivity"
            android:exported="false"
            android:label="Record a meeting" />

        <service
            android:name=".StreamService"
            android:exported="false"
            android:foregroundServiceType="microphone" />

        <!-- Records one meeting to a file. Same microphone type as the stream; the two
             never run together (starting a meeting stops the stream). -->
        <service
            android:name=".MeetingService"
            android:exported="false"
            android:foregroundServiceType="microphone" />

        <receiver
            android:name=".BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- Posts the "recording resumes soon" heads-up when ResumeWarning's alarm
             fires. Internal (the alarm PendingIntent is our own), so not exported. -->
        <receiver
            android:name=".ResumeWarningReceiver"
            android:exported="false" />

    </application>
</manifest>
